apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microservices-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: devopsdeveloper.site
    http:
      paths:
      - path: /service-A
        pathType: Prefix
        backend:
          service:
            name: service-A
            port:
              number: 80
      - path: /service-B
        pathType: Prefix
        backend:
          service:
            name: service-B
            port:
              number: 80
